# Runtime dependencies for score-reconciler.
# Install with:  pip install -r requirements.txt
#
# These mirror the [project].dependencies in pyproject.toml. Install them if
# you just want to run the tool from source without an editable install.
pandas>=2.0
openpyxl>=3.1
